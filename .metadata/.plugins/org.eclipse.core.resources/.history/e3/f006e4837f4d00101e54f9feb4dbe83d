package twoForTest;

// 1번 : 이중 for문 기본
public class TwoForTest01 {
	public static void main(String[] args) {
		
		for(int i = 1; i < 3; i++) { // 바깥 for문 : 가로
			System.out.println();
			for(int j = 1; j < 3; j++) { // 안쪽 for문 : 세로
				System.out.print("i : " + i + ", j : " + j);

			}
		}
		
		// 1) 바깥 for문 시작
		// 1-1) 초기식 i를 1로 초기화
		// 1-2) 조건식 i < 3 조건 검사 -> true
		// 1-3) 바깥 for문의 중괄호 영역 안으로 들어감(이제 안쪽 for문 실행)
		// 2) 안쪽 for문 실행
		// 2-1) 안쪽 for문의 초기식 j를 1로 초기화
		// 2-2) 안쪽 for문의 조건식 j < 3 조건 검사 -> true
		// 2-3) 안쪽 for문의 중괄호 영역 내로 들어와 출력 메소드 실행
		// 2-4) i : 1 j : 1인 상태가 출력됨.
		// 3) 안쪽 for문의 증감식으로 이동. j++ => 현재 j값 = 2
		// 3-1) 안쪽 for문의 조건식 j < 3 조건 검사 -> true
		// 3-2) 안쪽 for문의 중괄호 영역 내 문장 실행 -> i : 1 j : 2 출력
		// 3-3) 안쪽 for문의 증감식 j++로 이동. 현재 j값 = 3
		// 4) 안쪽 for문의 조건식 j < 3 조건 검사 => false => 안쪽 for문 종료
		// 4-1) 바깥쪽 for문 증감식으로 이동 => i++ => i값 : 2
		// 4-2) 바깥쪽 for문의 중괄호 영역(안쪽 for문의 int j=1 초기값으로 이동)
		// 5) 반복
		// ...
		// 6) 안쪽 for문의 증감식 수행 j++ => 현재 j값 : 3
		// 6-1) 안쪽 for문의 조건 검사 => j < 3 => false => 바깥쪽 for문으로 이동
		// 7) 바깥쪽 for문의 증감식으로 이동 => i++ => i의 값 : 3 => 조건검사 => false 
		// +) => 바깥쪽 for문 종료
		//		바깥 for문의 중괄호 영역 밖으로 이동
		
		
		
		
	}// main 종료
}
